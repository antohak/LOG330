  language: python
  python:
    - 3.6 
  install:
    - pip install -r requirements.txt
  # command to run tests
  script:  pytest 
  # travis doc -- https://docs.travis-ci.com/user/languages/python/ 
